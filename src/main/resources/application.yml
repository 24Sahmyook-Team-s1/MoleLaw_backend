spring:
  datasource:
    url: jdbc:mysql://molelaw-db.cpqukyis8rth.ap-northeast-2.rds.amazonaws.com:3306/?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: molelaw
    password: molelaw123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update  # 또는 create, validate 등 선택
    show-sql: true

  springdoc:
    api-docs:
      path: /v3/api-docs
    swagger-ui:
      path: /swagger-ui.html
      operationsSorter: method
      tagsSorter: alpha
      tryItOutEnabled: true

openai:
  api-key: ${OPENAI_API_KEY}
